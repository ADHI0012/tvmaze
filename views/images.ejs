<% layout('layouts/boilerplate') %>
<div class="d-flex flex-wrap justify-content-between gap-5">
  <% for (let image of images) {%> <% if (image && image.resolutions &&
  image.resolutions.medium && image.resolutions.medium.url){ %>
  <img
    class="img-fluid rounded"
    src="<%= image.resolutions.medium.url%>"
    alt=""
  />
  <% } %><% } %>
</div>
